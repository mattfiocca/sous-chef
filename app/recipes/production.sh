# OS specific ingredients to run (found in app/ingredients/[OS]/[INGREDIENT].sh)
use apt
use git
use nfs
use nginx
use phpfpm

# use a shared ingredient (found in app/ingredients/common/[INGREDIENT].sh)
# commom [INGREDIENT]